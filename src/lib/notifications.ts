
import type { Notification } from './types';
import { UserRole } from './types';

// This file is now deprecated and its content is merged into `lib/data.ts`.
// It is kept for backward compatibility with components that might still import it directly.
// The single source of truth for initial data is now `initialData` in `lib/data.ts`.

export const mockNotifications: Notification[] = [];
